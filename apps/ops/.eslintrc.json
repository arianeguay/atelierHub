{
  "$schema": "https://json.schemastore.org/eslintrc",
  "extends": ["../../.eslintrc.json"],
  "ignorePatterns": ["dist/**"],
  "overrides": [
    {
      "files": ["**/*.ts"],
      "rules": {}
    }
  ]
}
